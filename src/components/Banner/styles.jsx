import styled from "styled-components"
import "../../styles/index.css"

export const BannerStyle = styled.div`
  .banner {
    width: 100%;
    height: auto;
    margin: 5rem 0rem;
    pointer-events: none;
    user-select: none;
  }
`